<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.SplashAPI" parent="android:Theme.Material.Light.NoActionBar" />

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <!-- Splash Screen의 Background 색상.-->
        <item name="android:windowSplashScreenBackground" tools:targetApi="s">@color/white</item>

        <!-- SplashScreen에서 사용 될 아이콘. 설정하지 않으면 앱 아이콘이 사용된다. -->
        <item name="android:windowSplashScreenAnimatedIcon" tools:targetApi="s">@mipmap/ic_launcher_round</item>

        <!-- 앱 아이콘의 Default 배경 색상. 해당 값을 설정 하지 않으면 아무런 색도 보이지 않는다. -->
        <item name="android:windowSplashScreenIconBackgroundColor" tools:targetApi="s">@color/main</item>

        <!-- Animation이 그려지는 Max 시간. 해당 시간이 지나면 애니메이션이 동작하는 도중이라도 마지막 결과로 이동한다. -->
        <item name="android:windowSplashScreenAnimationDuration" tools:targetApi="s">5000</item>

        <!-- 화면 하단에 나오는 Branding 이미지. -->
        <item name="android:windowSplashScreenBrandingImage" tools:targetApi="s">@drawable/app_branding</item>
        <item name="android:windowSplashScreenBehavior" tools:targetApi="tiramisu">icon_preferred</item>
        <item name="postSplashScreenTheme">@style/Theme.SplashAPI</item>
    </style>
</resources>